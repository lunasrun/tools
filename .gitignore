# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Lunas compiler bindings, rebuilt from the external/lunas submodule
# (pnpm wasm:build). Node target drives the Vite build; web target runs in the
# browser playground.
wasm/

# Logs & OS cruft
*.log
.DS_Store

# Background subagent worktrees
.claude/worktrees/
